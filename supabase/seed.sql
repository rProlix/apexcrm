-- Intentionally empty.
--
-- Keep this file in source control because supabase/config.toml enables database seeding.
-- Production and tenant data must never be copied into the repository.
select 1;
